adapters:
  driftpixel:
    endpoint: http://rtb.driftpixel.live?pid={{SourceId}}&host={{Host}}&pbs=1
    meta-info:
      maintainer-email: developer@driftpixel.ai
      app-media-types:
        - banner
        - video
        - native
      site-media-types:
        - banner
        - video
        - native
      supported-vendors:
      vendor-id: 0
