adapters:
  mobfoxpb:
    endpoint: http://bes.mobfox.com/?c={route}&m={method}&key={key}
    meta-info:
      maintainer-email: support@mobfox.com
      app-media-types:
        - banner
        - video
      site-media-types:
        - banner
        - video
      supported-vendors:
      vendor-id: 311
