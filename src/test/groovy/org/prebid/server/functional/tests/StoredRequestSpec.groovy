package org.prebid.server.functional.tests

class StoredRequestSpec extends BaseSpec {

    def "PBS should only look up stored requests for provided AMP account"() {
        assert false
    }

    def "PBS should only look up stored requests for provided account"() {
        assert false
    }

    def "PBS be able to find unique stored request when account id is not provided"() {
        assert false
    }

    def "PBS should respond with an error when stored request id is not unique for account"() {
        assert false
    }

    def "PBS should respond with an error when stored request id is not unique and no account is provided"() {
        assert false
    }
}
